{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); case \"$f\" in */raw/*|*/_generated/*|*/node_modules/*) exit 0;; *.md|*.json) [ -f \"$f\" ] && pnpm exec oxfmt \"$f\" >/dev/null 2>&1; exit 0;; *) exit 0;; esac"
          }
        ]
      }
    ]
  }
}
