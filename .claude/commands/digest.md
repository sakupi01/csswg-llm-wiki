---
description: Write the weekly "This week in CSSWG" digest and rebuild the RSS feed
argument-hint: [--days N] [--until YYYY-MM-DD]
---

Read "## Workflows > ### Digest" in `AGENTS.md` and follow that procedure exactly.

Facts come only from the candidate pack (`_generated/digest-candidates/<until>.json`);
select and explain, never fabricate a source. Arguments (if any): **$ARGUMENTS**
