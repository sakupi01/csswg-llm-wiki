---
description: Write the weekly "This week in CSSWG" digest and rebuild the RSS feed
argument-hint: [--days N] [--until YYYY-MM-DD]
---

Read "## Workflows > ### Digest selection lens" and "### Digest" in `AGENTS.md` and follow
that procedure exactly.

Select with the **developer-impact lens** (would a working web developer want to know this? —
not comment count) and write each pick in the **4-field format** (Background / Why it matters /
Summary / Related). Facts
come only from the candidate pack (`_generated/digest-candidates/<until>.json`); select and
explain, never fabricate a source. Arguments (if any): **$ARGUMENTS**
